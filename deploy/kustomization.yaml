apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: novachess

resources:
  - namespace.yaml
  - storage.yaml
  - nats.yaml
  - worker.yaml
  - coordinator.yaml
  - dashboard.yaml
  - jobs.yaml

# Point this at your registry once the image is pushed.
images:
  - name: novachess
    newName: ghcr.io/piwi3910/novachess
    newTag: 4d82b9a
